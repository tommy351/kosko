{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "kosko#build": {
      "dependsOn": ["^build"],
      "outputs": ["deno.js"]
    }
  },
  "globalDependencies": ["tsconfig.json"]
}
